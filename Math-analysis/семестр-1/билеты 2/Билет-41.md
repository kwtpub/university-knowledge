## 41. Определение первообразной. Основные свойства первообразной. Таблица простейших интегралов.

### Первообразная функция. Неопределённый интеграл

**Определение.** Функция $F(x)$ называется **первообразной функцией** функции $f(x)$ на отрезке $[a, b]$, если в любой точке этого отрезка верно равенство:

$$F'(x) = f(x).$$

Надо отметить, что первообразных для одной и той же функции может быть бесконечно много. Они будут отличаться друг от друга на некоторое постоянное число:

$$F_1(x) = F_2(x) + C, \quad C = \text{const}.$$

**Определение.** **Неопределённым интегралом** от функции $f(x)$ называется совокупность всех её первообразных, которые записываются в виде:

$$\int f(x)\,dx = F(x) + C, \quad F'(x) = f(x).$$

Условием существования неопределённого интеграла на некотором отрезке является непрерывность функции на этом отрезке.

**Свойства неопределённого интеграла:**

1. Производная неопределённого интеграла равна подынтегральной функции:

$$\left(\int f(x)\,dx\right)' = f(x).$$

2. Дифференциал неопределённого интеграла равен подынтегральному выражению:

$$d\left(\int f(x)\,dx\right) = f(x)\,dx.$$

3. Неопределённый интеграл от производной равен самой функции плюс произвольная константа:

$$\int f'(x)\,dx = f(x) + C.$$

   Это свойство иногда записывают по-другому: $\int df(x) = f(x) + C$.

4. Постоянный множитель $k$ можно вынести за знак неопределённого интеграла:

$$\int k\,f(x)\,dx = k \int f(x)\,dx, \quad (k \neq 0).$$

5. Неопределённый интеграл от суммы двух функций равен сумме интегралов от этих функций:

$$\int [f(x) + g(x)]\,dx = \int f(x)\,dx + \int g(x)\,dx.$$

6. Свойство инвариантности: вид формулы интегрирования останется неизменным (инвариантным), если независимую переменную $x$ заменить любой дифференцируемой функцией $u(x)$, то есть:

$$\text{если } \int f(x)\,dx = F(x) + C, \text{ то } \int f(u)\,du = F(u) + C.$$

### Таблица простейших интегралов

| № | Интеграл | Значение |
|---|----------|----------|
| 1 | $\int x^\alpha\,dx$ | $\dfrac{x^{\alpha+1}}{\alpha+1} + C,\; \alpha \neq -1$ |
| 2 | $\int \dfrac{dx}{x}$ | $\ln|x| + C$ |
| 3 | $\int a^x\,dx$ | $\dfrac{a^x}{\ln a} + C$ |
| 4 | $\int e^x\,dx$ | $e^x + C$ |
| 5 | $\int \cos x\,dx$ | $\sin x + C$ |
| 6 | $\int \sin x\,dx$ | $-\cos x + C$ |
| 7 | $\int \dfrac{1}{\cos^2 x}\,dx$ | $\operatorname{tg} x + C$ |
| 8 | $\int \dfrac{1}{\sin^2 x}\,dx$ | $-\operatorname{ctg} x + C$ |
| 9 | $\int \dfrac{dx}{x^2+1}$ | $\operatorname{arctg} x + C$ |
| 10 | $\int \dfrac{dx}{\sqrt{1-x^2}}$ | $\arcsin x + C$ |
| 11 | $\int \operatorname{tg} x\,dx$ | $-\ln|\cos x| + C$ |
| 12 | $\int \operatorname{ctg} x\,dx$ | $\ln|\sin x| + C$ |
| 13 | $\int \dfrac{dx}{a^2+x^2}$ | $\dfrac{1}{a}\operatorname{arctg}\dfrac{x}{a} + C$ |
| 14 | $\int \dfrac{dx}{x^2-a^2}$ | $\dfrac{1}{2a}\ln\left|\dfrac{x+a}{x-a}\right| + C$ |
| 15 | $\int \dfrac{dx}{\sqrt{x^2 \pm a^2}}$ | $\ln\left|x + \sqrt{x^2 \pm a^2}\right| + C$ |
| 16 | $\int \dfrac{dx}{\sqrt{a^2-x^2}}$ | $\arcsin\dfrac{x}{a} + C$ |
| 17 | $\int \dfrac{1}{\cos x}\,dx$ | $\ln\left|\operatorname{tg}\!\left(\dfrac{x}{2}+\dfrac{\pi}{4}\right)\right| + C$ |
| 18 | $\int \dfrac{1}{\sin x}\,dx$ | $\ln\left|\operatorname{tg}\dfrac{x}{2}\right| + C$ |
| 19 | $\int e^{ax}\cos bx\,dx$ | $\dfrac{e^{ax}}{a^2+b^2}(b\sin bx + a\cos bx) + C$ |
| 20 | $\int e^{ax}\sin bx\,dx$ | $\dfrac{e^{ax}}{a^2+b^2}(a\sin bx - b\cos bx) + C$ |

---
