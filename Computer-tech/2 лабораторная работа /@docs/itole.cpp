/**************************************************************** 
*                     КАФЕДРА № 304 1 КУРС                      * 
*---------------------------------------------------------------* 
* Project Type  : Win32 Console Application                     * 
* Project Name  : Lab                                           * 
* File Name     : main.cpp                                      * 
* Language      : C/C++                                         * 
* Programmer(s) : Малохлебов Г.М, Лисицина В.С                  * 
* Modifyed By   :                                               * 
* Lit source    :                                               * 
* Created       : 07/11/25                                      * 
* Last Revision : 07/11/25                                      * 
* Comment(s)   : Табулирование функций (Вариант №6)             * 
****************************************************************/ 

#include <iomanip> 
#include <iostream> 
#include <cmath> 
using namespace std; 

// Основная программа 
int main() 
{ 
    double A = 0; // Левая граница отрезка 
    double B = 0; // Правая граница отрезка 
    int N = 0;    // Количество интервалов 
    double h = 0; // Величина шага 
    double x = 0; // Значение точки в местах разделения отрезка на интервалы 
    double F = 0; // Значение функции F(x) = (x^2 + 4)^(1/2) 
    double G = 0; // Значение функции G(x) = 4e^|x| - 1 

    setlocale(LC_ALL, "RU");     // Настройка локализации для корректного отображения русских символов 
    cout << fixed << setprecision(2); // Фиксированный формат вывода с 2 знаками после запятой 

    // Ввод левой границы отрезка 
    cout << "Введите A: "; 
    cin >> A; 

    // Ввод правой границы отрезка 
    cout << "Введите B: "; 
    cin >> B; 

    // Входной контроль: проверка условия A < B 
    while (A > B) 
    { 
        // Вывод сообщений об ошибке 
        cout << " Ошибка ввода значений" << endl; 
        cout << " Были введены некорректные значения A и B" << endl; 
        cout << " введите любые значения A и B, которые" << endl; 
        cout << " будут удовлетворять условию: (A < B)" << endl; 
        
        // Повторный ввод значений 
        cout << "Введите A: "; 
        cin >> A; 
        cout << "Введите B: "; 
        cin >> B; 
    } 

    // Ввод количества интервалов 
    if (A != B){
    cout << "Введите N: "; 
    cin >> N; 

    // Входной контроль: проверка условия N > 0 
    while (N <= 0) 
    { 
        // Вывод сообщений об ошибке 
        cout << " Ошибка ввода значений" << endl; 
        cout << " Было введено некорректное значение N" << endl; 
        cout << " введите любое значение N, которое" << endl; 
        cout << " будет удовлетворять условию: (N > 0)" << endl; 
        
        // Повторный ввод значения 
        cout << "Введите N: "; 
        cin >> N; 
    } 
    }

    // Эхопечать начальных значений 
    cout << "A = " << A << ", B = " << B << ", N = " << N << endl; 

    if (A != B){h = (B - A) / N;} // Вычисление шага разбиения отрезка 

    // Вывод верхней границы и заголовка таблицы 
    cout << "┌─────────┬───────────┬────────┬────────────────────┐\n"; 
    cout << "│    N    │     x     │   F(x) │        G(x)        │\n"; 

    // Цикл вычисления и вывода значений функций для каждой точки 
    for (int i = 0; i <= N; i++) 
    { 
        x = A + i * h;                // Вычисление текущей точки на отрезке 
        F = sqrt(x * x + 4);          // Подсчет первой функции F(x) = (x²+4)^1/2 
        G = 4 * exp(fabs(x)) - 1;     // Подсчет второй функции G(x) = 4*e^|x|-1 

        // Вывод горизонтальной разделительной линии 
        cout << "├─────────┼───────────┼────────┼────────────────────┤\n"; 

        // Вывод строки с результатами: номер точки, x, F(x), G(x) 
        cout << "│ " << setw(7) << i + 1 
             << " │ " << setw(9) << x 
             << " │ " << setw(6) << F 
             << " │ " << setw(18) << G << " │\n"; 
    } // for i 

    // Вывод нижней границы таблицы 
    cout << "└─────────┴───────────┴────────┴────────────────────┘\n"; 
} // main() 

/********** End of main.cpp file **********/


